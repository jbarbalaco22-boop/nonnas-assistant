anthropic>=0.40
python-dotenv>=1.0
fastapi>=0.115
uvicorn>=0.30

# Shared QBO/Shopify connectors, also used by nonnas-finance-audit and nonnas-daily-operator.
# Public repo, so no auth needed to install this on a build server like Render's. Pinned to an
# exact commit (not just the repo URL) so Render's build cache actually picks up shared-repo
# changes - an unpinned git+https URL let a real deploy run for a while against a stale install
# after a nonnas-shared-only push, since nothing in this repo's requirements.txt had changed.
git+https://github.com/jbarbalaco22-boop/nonnas-shared.git@26051f4291b06ae627b516e68439ae0a2b2bb3d7
